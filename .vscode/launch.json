{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Linux w/ Debugger",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/x86_64-unknown-linux-gnu/debug/symbiants_pkg",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}